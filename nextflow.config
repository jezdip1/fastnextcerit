// nextflow.config

k8s {
  namespace           = 'jezdik-ns'
  runAsUser           = 1000
  computeResourceType = 'Job'
  cpuLimits           = true
  storageClaimName    = 'pvc-seg'
  storageMountPath    = '/mnt/data'
  launchDir           = '/mnt/data/launch'
  workDir             = '/mnt/data/work'
  // dump out every Pod spec nextflow submits
  printPodSpec = true
}

executor {
  queueSize = 30
}

params {
  input_dir = '/mnt/data/input'
  license   = '/mnt/data/license.txt'
}

process {
  executor  = 'k8s'
  container = 'cerit.io/nextflow/nextflow:24.04.4'
  cpus      = 1
  memory    = '12 GB'

  // === everything in here only for GPU jobs ===
  withLabel: gpujob {
    container = 'jezdip1/fastsurfer-cerit:latest'
    cpus      = 2
    memory    = '12 GB'

    ext.k8s = [
      // ask for 1 GPU
      requests: [ 'nvidia.com/gpu': '1', cpu: '2', memory: '10Gi' ],
      limits:   [ 'nvidia.com/gpu': '1', cpu: '2', memory: '12Gi' ],

      // (optionally pin to GPU-labelled nodes—remove if your cluster doesn't use this)
      nodeSelector: [ 'nvidia.com/gpu.present':'true' ],

      // keep your securityContext
      securityContext: [
        runAsNonRoot:             true,
        runAsUser:                1000,
        allowPrivilegeEscalation: false,
        capabilities: [ drop: ['ALL'] ],
        seccompProfile: [ type: 'RuntimeDefault' ]
      ]
    ]
  }
}
